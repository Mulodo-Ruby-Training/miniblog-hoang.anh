<div id="navbar" class="navbar-collapse collapse col-md-4">
  <ul class="nav navbar-nav navbar-right">
    <li>
      <%= link_to(controller: 'posts', action: 'all') do  %>
      <%= content_tag(:i,"",class: "glyphicon glyphicon-book").html_safe %>
      <%= "Blog" %>
      <% end %>
    </li>
    <% if !session[:id] && !session[:token] %>
    <li>
      <%= link_to(controller: 'users', action: 'signin') do %>
      <%= content_tag(:i,"",class:"glyphicon glyphicon-log-in").html_safe %>
      <%= "Login" %>
      <% end %>
    </li>
    <% else %>
    <li><%= link_to(controller: 'posts', action: 'new') do %>              
      <%= content_tag(:i, "", class: "glyphicon glyphicon-pencil").html_safe %>
      <%= "Write now" %>
      <% end %>
    </li>
    <li class="dropdown">
      <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="true"><i class="glyphicon glyphicon-user"></i> My Account&nbsp;<span class="glyphicon glyphicon-chevron-down"></span></a>
      <ul class="dropdown-menu" role="menu">
        <li><%= link_to("Update Info", controller: 'users', action: 'edit') %></li>
        <li><%= link_to("Change Password", controller: 'users', action: 'change_password') %></li>
        <li><%= link_to("Manage Post", controller: 'posts', action: 'manage') %></li>
        <li><%= link_to("Logout", controller: 'users', action: 'logout') %></li>
      </ul>
    </li>
    <% end %>

    <% if !session[:id] && !session[:token] %>
    <li>
      <%= link_to(controller: 'users', action: 'signup') do %>
      <%= content_tag(:i,"",class:"glyphicon glyphicon-plus").html_safe %>
      <%= "Create a account" %>
      <% end %>
    </li>
    <% end %>

  </ul>
</div>